import React from 'react';
import Content from './Content';
const Hero = () => {
  return (
    <div style={{ background: `#000`, minHeight: '100vh' }}>
      <Content />
    </div>
  );
};

export default Hero;
